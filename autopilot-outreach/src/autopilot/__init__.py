"""Autopilot Outreach — autonomous local-business website discovery & sales pipeline.

Runs fully offline in `mock` mode (no API keys, stdlib only) so the whole
funnel is easy to test and debug. Swap providers to `live` mode to wire in
real Google Places / voice / email / payment backends.
"""

__version__ = "0.1.0"
